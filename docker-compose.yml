services:
  web:
    build:
      context: .
      dockerfile: Dockerfile
    image: ssts-integrated-webapp
    container_name: ssts-webapp
    ports:
      - "8081:8081"
    environment:
      - PORT=8081
    restart: unless-stopped